{% style %}
    {% if custom_heading_font %}
        @font-face {
            font-display: swap;
            font-family: brandheader;
            src: url("{{ 'Arial-BoldMT.woff2' | asset_url }}") format("woff2"); 
        }
    {% endif %}
{% endstyle %}